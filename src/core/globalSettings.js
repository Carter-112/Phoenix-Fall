/**
 * Global Settings
 * This file contains global settings and flags used throughout the game
 */

// Whether to use the Universal UI instead of world-specific UI
window.universalUIEnabled = true;

// Default particle density settings
window.particleDensity = 1.0; // 1.0 = full, 0.5 = half, etc.

// Debug mode - set to true to enable console logs and debug features
window.debugMode = false;

// Default sound settings
window.soundEnabled = true;
window.musicEnabled = true;
window.soundVolume = 0.7;
window.musicVolume = 0.5;

console.log('Global settings initialized');
